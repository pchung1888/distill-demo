# Base URL of the distill FastAPI backend this frontend talks to.
# Copy this file to .env.local for local development.
# Default (below) matches `uv run uvicorn distill.api.app:app --port 8000`
# run locally from the distill repo.
NEXT_PUBLIC_DISTILL_API_URL=http://localhost:8000
